# Only publish PostgreSQL to localhost for development with the Vite client.
# docker compose -f compose.yaml -f compose.dev.yaml up -d database
services:
  database:
    ports: ["127.0.0.1:5432:5432"]
