# 性能优化执行进度

更新时间：2026-09-14。按 `performance-optimization-commands.md` 执行，每次仅一个阶段。

## 当前：阶段 1、2 实现已落地，等待最后统一验收

**用户于 2026-09-14 更新执行方式：先做开发，所有步骤开发完成后再统一测试。** 该要求优先于原命令集中的“每阶段立即验证”。后续不主动运行测试、lint、构建、浏览器或性能采样；可以继续读写源码、实现功能、整理资产与记录待验项。

- 阶段 0：生产正确性修复和历史 80 次生产基线已完成。旧红楼规则测试已对齐当前玩法并扩充为 20 项，技能逻辑未改。暂停测试前已有 38 项组合测试通过，不能沿用为阶段 2 的验证结论。
- 阶段 1：完整字库分片、已选字体预热、130 项缩略资源、同步背景及可见失败回退、真实静态服务压缩和后续验收工具已实现。新 core 构建、字体/图片浏览器验收及正式前后采样待最后执行。
- 阶段 2：已实现人物数据分页，接入身份局/斗地主自由选将及适用只读目录；选中、禁用、焦点状态和引擎适配、完整 Button 兼容回退、按需分批搜索、16 条缓存与取消/Worker 超时处理已落地。未执行任何阶段 2 测试或构建，不宣称性能达标。
- 阶段 3–8 尚未开发。当前改动未提交、推送或部署。
- 截图反馈修复：移除名将杀子包私有开关产生的伪扩展登记，保留用户配置；修复分页势力名称显示 HTML 源码。已整理待提交文件，将字体生成审计移入忽略目录，保留有引用的缩略图和字体分片。补充登记回归用例，仍未运行测试、构建或浏览器验证。

实现边界、文件、阶段 1 生成统计、既有证据和最终统一验收清单见 [阶段 1、2 开发记录](performance-stages-1-2-development.md)。

下一条建议命令：

```text
请继续执行阶段 3 的开发：按性能指导手册处理 onfree、菜单和选将后台长任务。沿用先开发后统一测试的要求，本轮不运行测试、lint、构建或性能采样；完成源码和进度记录后停止，不部署。
```

## 阶段 0 生产正确性修复与基线（历史记录）

以下为阶段 1 开始前的记录；其中旧规则测试失败已由上方当前记录更新，外部待验仍保留。

源码起点为 `919e48096`，仍在 `04_FixUIAndBUG`。本轮生产启动、旧式 step 构建和默认扩展热启动的三项阻塞已修复；**本地核心检查通过，整体验收仍有既有红楼规则单测失败与外部待验，不能宣称工程已全面验收。** 阶段 1–8 未开始。

- 普通 core 构建保留经 `/noname.js` 的自引用，解决 `GamePromises` 循环初始化；独立包关闭会删除 step 标记的 tree-shaking。模块路径和公共入口保留，旧失败用例保留为独立负向对照。
- 将“红楼幻梦”的错误运行标识/资源路径对齐真实目录“红楼幻境”，兼容迁移旧清单与私有选项，保留真实标识下的用户开关和旧键。
- 新增包含 JIT 自动刷新的 `lobbyReadyWallMs`。精简与默认扩展各 20 冷 + 20 热，共 80 次生产流程通过，无 pageerror/弹窗，两组命令退出 0；默认扩展关闭采集的冷/热复测也通过。
- 默认扩展完整大厅启动 p50/p95：冷 2725/3596 ms、热 2034/2431 ms。修复前生产无法进入大厅，没有有效的百分比提速；旧开发数据与本次内容/渠道不同，不直接比较。
- 工具单测 6/6、扩展登记/迁移/资源测试 11/11、独立语义门槛和局部 lint 通过。额外红楼规则旧单测 13/18 失败；修改前隔离对照为相同 13 项失败，保留为待修事项。资源缺失、真机、用户实际配置、线上、完整规则/联机/回放与长期运行仍待验。
- 测试使用隔离数据及 8082，自建服务和浏览器已关闭；既有 8081 服务保留。本轮未提交、推送或部署。

修改涉及 `apps/core/scripts/build.ts`、`noname/init/organizedExtensions.ts`、红楼幻境的 `extension.js/info.json`、登记回归，以及 `scripts/performance/run.ts/semantics.ts/summarize.ts` 和配套文档。完整证据、测试命令、失败说明及回退方式见 [生产正确性补充报告](performance-stage-0-correctness-report.md)。

## 对话中断后的接续记录（2026-09-14）

- 已从旧任务与本地证据恢复上下文：原授权为阶段 0，最后停在无埋点生产对照的报告收尾。当前分支 `04_FixUIAndBUG`，HEAD 仍为 `78069f967d314a9804f9d446f63d7a3636d3afbd`。
- 核对原始摘要：精简与默认扩展各 20 冷 + 20 热，共 80 次流程通过，无 pageerror；资源错误仍保留。审计结果：`output/performance/resume-evidence-audit.json`。
- 找回并核验此前尚未归档到报告的无埋点对照：`output/performance/2026-09-14T02-18-00-067Z-production-minimal/`。4 个被恢复文件与 HEAD 字节一致，构建成功，但仍因 `GamePromises` 初始化错误无法进入大厅。该故障并不依赖性能埋点，尚未定位具体构建修复方案。
- 本次重跑工具单测 6/6 通过；独立语义检查仍有两个 buildIndividual 旧式 step 失败。关闭采集的精简冷/热流程各 1 次通过，无 pageerror/弹窗，命令仍因构建语义门槛退出 1。
- 8081 有既存服务，本次隔离浏览器复测使用 8082，测试结束后已关闭自建服务。两次流程只是当前工作区的功能复测，不并入原 80 次性能统计。详细命令、证据见阶段 0 报告第 7 节。
- 当前工作区还包含扩展恢复、登记、配置导入事务等已有未提交改动；它们不属于阶段 0 性能优化交付，本次保留。当前源码与早期采样快照有差异，未重新采集默认扩展正式基线，也未重新构建当前工作区的生产产物。

**历史状态：当时仅完成中断收尾，生产启动和构建语义仍阻塞。后续处理见本文件顶部当前记录。**

## 阶段 0：首次基线与正确性保护（历史记录）

**状态：工具已实现、本地基线已执行；正确性门槛存在失败，验收未通过。** 不能据此宣称工程已流畅或生产可用。阶段 1–8 尚未实施。

本次访问条件采用用户指定的 `http://localhost:8081` 和本机 Chrome。测试使用隔离浏览器数据、双人身份局和固定随机种子；精简包与仓库默认扩展分别记录。用户真实存档/扩展组合、移动真机、线上环境仍待验。

### 已实现

- `apps/core/noname/util/performance.ts`：仅 `?perf=1` 启用，记录启动阶段、资源、长任务、事件时间及编译缓存；环形缓存和停止接口控制记录增长。
- `apps/core/index.html`、`noname/entry.ts`、`noname/init/index.ts`、`ContentCompiler.ts`：增加诊断埋点，保留现有加载和编译策略。
- `scripts/performance/run.ts`、`environment.ts`、`seed.ts`：localhost:8081 自动化交互、隔离配置、冷/热样本、HAR、首对 CPU trace、截图、错误清单、源码哈希与摘要。真实断言涵盖选将、翻页、搜索、首个合法操作及手牌选中/取消。
- `semantics.ts`、`step-fixtures.js`、`check-semantics.ts`、`semantic-page.*`：真实编译器执行源码及 Vite 构建 fixture，验证旧式步骤和 async；Terser 为负向对照。
- `collector.test.ts`、`build.ts`、`summarize.ts` 及根目录 `perf:*` 命令：采集器回归、隔离普通生产构建和结果汇总。

运行方式见 [测试工具说明](../scripts/performance/README.md)。实测数据、证据路径和验收限制见 [阶段 0 报告](performance-stage-0-report.md)。阶段 0 建立的是后续对比基线，没有实施性能优化，故“优化后数值”尚无。

### 已发现的正确性问题

首次采样时 `buildIndividual` 为 `minify:false`、`treeshake:true`。实际 Vite fixture 产物丢失旧式 step 标记，goto/redo/跨步结果验证失败；源码与 `buildSelf` 对照通过。该失败保留为阻止不安全构建的门槛，不能通过忽略断言或放宽验收消除。

普通生产产物复测还出现 `Cannot access 'GamePromises' before initialization`，无法进入大厅；生产性能验收保持失败/未测成功。开发环境 80 次流程断言通过，关闭采集冷/热各 1 次也通过，工具单测 6/6、局部 lint 和采集模块类型检查通过。详见报告中的独立证据路径，不能用这些通过项覆盖生产和语义失败。

### 回退与后续命令

日常关闭诊断只需移除 URL 的 `perf=1`。需要撤回阶段 0 时，按本次差异移除埋点导入/调用、HTML 标记和 `perf:*` 脚本，再移除新增工具；保留分析手册、命令集和已有用户改动，不使用全仓库 reset。原始证据在被 Git 忽略的 `output/performance/`，请备份后再清理。

当时建议先完成生产正确性阻塞处理；下述命令现已接续执行，下一步请使用顶部最新命令：

```text
请读取性能优化指导手册、执行命令集、阶段 0 报告和进度记录，处理阶段 0 报告中生产启动及旧式 step 构建语义的阻塞。保留失败用例，定位并实施最小必要修复，重新运行语义门槛和真实生产流程，补齐生产基线；不要扩大到全部阶段 6 或直接部署。更新报告后停止。
```

后续执行阶段 1 时仍使用命令集中的完整命令。资源优化与构建正确性应各自验收，不能用开发模式变快替代生产验证。
